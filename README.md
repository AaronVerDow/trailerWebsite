# Trailer Website

This theme requires a newer version of hugo:

```
nix-shell -I nixpkgs=channel:nixos-unstable -p hugo
```

Need to find get a working flake.nix for this.


Test with:

```
hugo server
```


